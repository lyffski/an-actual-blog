---
const pubDate = Astro.props.date;

/* function formatDate(pubDate:any):string {
      if(pubDate === undefined) return "";
      const dateParts = pubDate.split('.');
      const day = dateParts[0];
      const month = dateParts[1];
      const year = dateParts[2].slice(-2);
      return `~ ${day}/${month}/${year}`;
    } */

function formatDate(pubDate: any) {
  if (pubDate === undefined) return;
  const dateParts = pubDate.split(".");
  const day = dateParts[0];
  const month = new Intl.DateTimeFormat("en-US", { month: "short" }).format(
    new Date(dateParts[2], dateParts[1] - 1, dateParts[0])
  );
  const year = dateParts[2];
  return `${day}, ${month} ${year} `;
}
const x1 = "Published: ";
const x = formatDate(pubDate);
---
<style>
    .publishedDate {
        display: flex;
        justify-content: space-between;
        margin-bottom: 1rem;
        align-items: right;
    }
    .bold{
        font-weight: bold;
    }
</style>

<div class="publishedDate">
    {x1}
</div>

<script src="https://kit.fontawesome.com/20a23bab30.js" crossorigin="anonymous"></script>